plugins {
    id("convention.arch.viewmodel")
}

android {
    namespace = "com.ramruizni.deepseekpokeappfour.features.pokemon.viewmodel"
}

dependencies {
    // TODO: Add domain module dependency when available
    // implementation(project(":pokemon:domain"))
}