package com.example.deepseekpokeappten.database.converters

import androidx.room.TypeConverter

class PokemonTypeConverters {
    // Room handles String to String automatically, 
    // so we don't need explicit converters for JSON strings
}